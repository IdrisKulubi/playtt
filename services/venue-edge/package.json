{
  "name": "@playtt/venue-edge",
  "version": "0.2.6",
  "private": true,
  "type": "module",
  "description": "PlayTT VenueEdge service — venue-local replay capture and direct R2 upload",
  "scripts": {
    "start": "tsx src/index.ts start",
    "simulate": "tsx src/index.ts simulate",
    "enroll": "tsx src/index.ts enroll",
    "setup": "tsx src/index.ts setup",
    "build": "node scripts/build.mjs",
    "pack:dry-run": "node scripts/pack-dry-run.mjs",
    "test": "node --import tsx --test test/*.test.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "esbuild": "^0.25.9",
    "tsx": "^4.20.5",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=22"
  }
}
