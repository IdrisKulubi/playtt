import type { ReactNode } from "react"

import "../admin/admin.css"

export const dynamic = "force-dynamic"

export default function NvrLayout({ children }: { children: ReactNode }) {
  return children
}
